# Track A: Name OCR v2 Implementation Plan

> **Worktree:** `.worktrees/feature-cartograph-name-ocr-v2`  
> **Branch:** `feature/cartograph-name-ocr-v2`

**Goal:** Improve city/alliance name OCR on existing cartograph bands so v9 catalog gains real named pins without click-popup OCR.

**Tech:** Python 3.12+, OpenCV, pytesseract and/or EasyOCR, pytest, existing `entities` merge.

## Files

| File | Change |
|------|--------|
| `ks/cartograph/labels.py` | Preprocess, confidence, better line grouping; optional EasyOCR path |
| `ks/cartograph/entities.py` | Use richer OCR tuples; keep unknown for weak text |
| `ks/cartograph/pipeline.py` | Digitize path uses improved labels |
| `tests/test_cartograph_labels.py` | New focused OCR unit tests (synthetic text bands) |
| `tests/test_cartograph_entities.py` | OCR confidence / city inference |

## Tasks

1. Failing tests: synthetic band with `[ROY]lord123` recovers city label + conf.  
2. Implement preprocess (contrast/upscale) + keep Tesseract; add EasyOCR fallback when available.  
3. Wire confidence into `detect_frame_observations`.  
4. Run focused tests; optional offline re-OCR of one v9 frame for smoke.  
5. Do **not** implement ADB zoom in this track (follow-up). Do **not** edit `sprites.py`.

Do not commit unless asked.
