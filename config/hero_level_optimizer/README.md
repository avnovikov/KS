# Hero level optimizer reference data

Scraped from [Kingshot Hero Level chart](https://ks.h5joy-games.com/guides/hero-level/) (2026-08-02).

| File | Purpose |
|------|---------|
| `hero_level_xp_costs.yaml` | XP to reach each level 1–80 |
| `hero_level_power.yaml` | `power_factor` = deployment capacity at that level |

Max hero level: **80**.
